```c++
// 26 - Convert All String Characters to Uppercase and Lowercase
#include <iostream>
#include <cmath>
#include <string>

using namespace std;

string ReadString()
{
    string S1;
    cout << "Please Enter Your String? " << endl;
    getline(cin, S1);

    return S1;
}
string LowerAllWord(string S)
{
    bool IsFirstLetter = true;

    for (short i = 0; i < S.length(); i++)
    {
            S[i] = tolower(S[i]);
    }

    return S;
}
string UpperAllWord(string S)
{
    bool IsFirstLetter = true;

    for (short i = 0; i < S.length(); i++)
    { 
        S[i] = toupper(S[i]);
    }

    return S;
}
int main()
{
    string S1 = ReadString();

    cout << "\nString After Upper : " << endl;
    S1 = UpperAllWord(S1);
    cout << S1 << endl;

    cout << "\nString After Lower : " << endl;
    S1 = LowerAllWord(S1);
    cout << S1 << endl;

    return 0;
}

```
