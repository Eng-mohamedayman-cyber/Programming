```c++
//#6 — Calculate the Sum of Digits
#include<iostream>
#include<string>
using namespace std;
int ReadPositiveNumber(string Message)
{
    int Number ;
    do
    {
        cout<<Message<<endl;
        cin>>Number;
    }while(Number <= 0);
    return Number;
}
void PrintDigits(int Number)
{
    int Remainder ;
    while(Number > 0 )
    {
        Remainder = Number % 10 ;
        Number /= 10 ;
        cout<<Remainder<<endl;
    }
}
int SumOfDigits(int Number)
{
    int Remainder ;
    int Sum = 0;
    while(Number > 0 )
    {
        Remainder = Number % 10 ;
        Number /= 10 ;
        Sum += Remainder ;
    }
    return Sum;
}
int main()
{
    int Number = ReadPositiveNumber("Please enter Positive number ? ") ;
 //   PrintDigits(Number);
    cout<<"The sum of Digits = "<<PrintSumOfDigits(Number)<<endl;
    return 0 ;
}

```
