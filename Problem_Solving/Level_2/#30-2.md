```c++
//#30 — Add Two Arrays Element by Element
#include <iostream>
#include<string>
#include<cmath>
#include<cstdlib>
#include<ctime>
using namespace std;
int ReadPositiveNumber(string Message)
{
    int Number ;
    do
    {
        cout<<Message<<endl;
        cin>>Number;
    }while(Number <= 0);
    return Number;
}
int RandomNumber(int From , int To)
{
    int RanNum = rand() % (To - From + 1 ) + From ;
    return RanNum;
}
void FillArrayByRandomNumber(int Arr[100] ,int  ArrLength)
{
    for(int i = 0 ; i < ArrLength ; i++ )
    {
        Arr[i]= RandomNumber(1,100);
    }
    cout<<endl;
}
void PrintArray(int Arr[100],int ArrLenggth )
{

    for(int i = 0 ; i < ArrLenggth ; i++)
    {
        cout<<Arr[i]<<" ";
    }
    cout<<endl;
}
void SumOfArr1AndArr2Element(int Arr1[100] , int Arr2[100] , int ArrSum[100] , int ArrLength )
{
    for(int i = 0 ; i < ArrLength ; i++ )
    {
        ArrSum[i] = Arr1[i] + Arr2[i] ;
    }

}
int main()
{
    srand((unsigned)time(NULL));

    int Arr[100] , Arr2[100] , ArrSum[100] ;
    int ArrLength = ReadPositiveNumber("Hoe many element? ");

    FillArrayByRandomNumber(Arr,ArrLength);
    FillArrayByRandomNumber(Arr2,ArrLength);

    SumOfArr1AndArr2Element(Arr,Arr2,ArrSum,ArrLength);

    cout<<"\nArray 1 Elements : \n";
    PrintArray(Arr,ArrLength);

    cout<<"\nArray 2 Element : \n";
    PrintArray(Arr2,ArrLength);


    cout<<"\nSum pf array 1 and array 2 element : \n";
    PrintArray(ArrSum,ArrLength);

    cout<<endl;
    return 0;
}

```
