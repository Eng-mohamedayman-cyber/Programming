```c++
//#32 — Copy an Array in Reverse Order
#include <iostream>
#include<string>
#include<cmath>
#include<cstdlib>
#include<ctime>
using namespace std;

int RandomNumber(int From , int To)
{
    int RanNum = rand() % (To - From + 1 ) + From ;
    return RanNum;
}
void FullArrayByRandomNumber(int Arr[100] ,int  &ArrLength)
{
    cout<<"\nEnter number of element : "<<endl;
    cin>>ArrLength;

    for(int i = 0 ; i < ArrLength ; i++ )
    {
        Arr[i]= RandomNumber(1,100);
    }
    cout<<endl;
}

void PrintArray(int Arr[100],int ArrLenggth )
{

    for(int i = 0 ; i < ArrLenggth ; i++)
    {
        cout<<Arr[i]<<" ";
    }
    cout<<endl;
}
void CopyArrayInReversedOrder(int ArrSourse[100] , int ArrDestination[100] , int ArrLength )
{
    for(int i = 0 ; i < ArrLength ; i++)
    {
        ArrSourse[i] = ArrDestination[ ArrLength - 1 - i ];
    }
}
int main()
{
    srand((unsigned)time(NULL));

    int Arr[100] , Arr2[100] , ArrLength ;

    FullArrayByRandomNumber(Arr,ArrLength);

    CopyArrayInReversedOrder(Arr,Arr2,ArrLength);

    cout<<"Array 1 Element : "<<endl;
    PrintArray(Arr,ArrLength);

    cout<<"Array 2 element after copy : "<<endl;
    PrintArray(Arr2,ArrLength);

    return 0 ;
}

```
