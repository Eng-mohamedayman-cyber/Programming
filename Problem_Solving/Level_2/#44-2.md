```c++
//#44 — Count Positive Numbers in an Array
#include<iostream>
#include<cstdlib>
#include<ctime>
#include<string>
#include<cmath>
using namespace std;
int RandomNumber(int From , int To)
{
    int RanNum = rand() % (To - From + 1 ) + From ;
    return RanNum;
}
void FullArrayByRandomNumber(int Arr[100] ,int  &ArrLength)
{
    cout<<"\nEnter number of element : "<<endl;
    cin>>ArrLength;

    for(int i = 0 ; i < ArrLength ; i++ )
    {
        Arr[i]= RandomNumber(-100,100);
    }
    cout<<endl;
}
void PrintArray(int Arr[100],int ArrLenggth )
{

    for(int i = 0 ; i < ArrLenggth ; i++)
    {
        cout<<Arr[i]<<" ";
    }
    cout<<endl;
}
int PositiveNumberCounter(int Arr[100],int ArrLength)
{
    int Counter = 0 ;

    for(int i = 0 ; i < ArrLength ; i++ )
    {

        if(Arr[i] >= 0)
        {
            Counter++;
        }
    }

    return Counter ;
}
int main()
{
    srand((unsigned)time(NULL));

    int Arr[100] , ArrLength = 0 ;
    FullArrayByRandomNumber(Arr,ArrLength);

    cout<<"\nArray 1 Element: \n" ;
    PrintArray(Arr,ArrLength);
    cout<<"\Positive Number Counter = ";
    cout<<PositiveNumberCounter(Arr,ArrLength)<<endl;

    return 0;
}

```
