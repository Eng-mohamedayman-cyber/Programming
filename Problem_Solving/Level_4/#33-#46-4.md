```c++
//#33to#46-4
/*
Write a program to read a date and make itDecrease date as follows:
DecreaseDateByOneDay
DecreaseDateByXDays
DecreaseDateByOneWeek
DecreaseDateByXWeeks
DecreaseDateByOneMonth
DecreaseDateByXMonths
DecreaseDateByOneYear
DecreaseDateByXYears
DecreaseDateByXYearsFaster
DecreaseDateByOneDecade
DecreaseDateByXDecades
DecreaseDateByXDecadesFaster
DecreaseDateByOneCentury
DecreaseDateByOneMillennium
*/
#include <iostream>
#include <string>

using namespace std;

short ReadYear()
{
	short Year;

	cout << "Please enter a year? ";
	cin >> Year;

	return Year;
}
short ReadMonth()
{
	short Month;

	cout << "Please enter a Month? ";
	cin >> Month;

	return Month;
}
short ReadDay()
{
	short Day;

	cout << "\nPlease enter a Day? ";
	cin >> Day;

	return Day;
}
bool IsLeapYear(short Year)
{
	return (Year % 4 == 0 && Year % 100 != 0) || (Year % 400 == 0);
}
short NumberOfDaysInAMonth(short Month, short Year)
{
	if (Month < 1 || Month > 12)
		return 0;

	int NumberOfDays[13] = { 0,31,28,31,30,31,30,31,31,30,31,30,31 };
	return (Month == 2) ? ((IsLeapYear(Year)) ? 29 : 28) : NumberOfDays[Month];
}
int TotalDaysFromBeginingOfYear(short Day, short Month, short Year)
{
	int TotalDays = 0;

	for (int i = 1; i < Month; i++)
	{
		TotalDays += NumberOfDaysInAMonth(i, Year);
	}

	TotalDays += Day;

	return TotalDays;
}
struct sDate
{
	short Year;
	short Month;
	short Day;
};
sDate DateAddDays(short Days, sDate Date)
{
	short RemainDays = Days + TotalDaysFromBeginingOfYear(Date.Day, Date.Month, Date.Year);
	short MonthDays = 0;

	Date.Month = 1;

	while (true)
	{
		MonthDays = NumberOfDaysInAMonth(Date.Month, Date.Year);

		if (RemainDays > MonthDays)
		{
			RemainDays -= MonthDays;
			Date.Month++;

			if (Date.Month > 12)
			{
				Date.Month = 1;
				Date.Year++;
			}
		}
		else
		{
			Date.Day = RemainDays;
			break;
		}
	}

	return Date;
}
sDate ReadFullDate()
{
	sDate Date;

	Date.Day = ReadDay();
	Date.Month = ReadMonth();
	Date.Year = ReadYear();

	return Date;
}
bool IsDate1BeforeDate2(sDate Date1, sDate Date2)
{
	return (Date1.Year < Date2.Year) ? true : ((Date1.Year == Date2.Year) ? (Date1.Month < Date2.Month ? true : (Date1.Month == Date2.Month ? Date1.Day < Date2.Day : false)) : false);
}
bool IsLastDayInMonth(sDate Date)
{
	return (Date.Day == NumberOfDaysInAMonth(Date.Month, Date.Year));
}
bool IsLastMonthInYear(short Month)
{
	return (Month == 12);
}
sDate DecreaseDateByOneDay(sDate Date)
{
	if (Date.Day == 1)
	{
		if (Date.Month == 1)
		{
			Date.Year--;
			Date.Month = 12;
			Date.Day = 31;
		}
		else
		{
			Date.Month--;
			Date.Day = NumberOfDaysInAMonth(Date.Month, Date.Year);
		}
	}
	else
	{
		Date.Day--;
	}

	return Date;
}
sDate DecreaseDateByXDays(short NumOfDays, sDate Date)
{
	for (int i = 1; i <= NumOfDays; i++)
	{
		Date = DecreaseDateByOneDay(Date);
	}

	return Date;
}
sDate DecreaseDateByOneWeek(sDate Date)
{
	for (int i = 1; i <= 7; i++)
	{
		Date = DecreaseDateByOneDay(Date);
	}

	return Date;
}
sDate DecreaseDateByXWeeks(short NumOfWeek, sDate Date)
{
	for (int i = 1; i <= NumOfWeek; i++)
	{
		Date = DecreaseDateByOneWeek(Date);
	}

	return Date;
}
sDate DecreaseDateByOneMonth(sDate Date)
{
	short NumOfDays = 0;

	if (Date.Month == 1)
	{
		Date.Year--;
		Date.Month = 12;
	}
	else
		Date.Month--;
		
	
	NumOfDays = NumberOfDaysInAMonth(Date.Month, Date.Year); 
	if (Date.Day > NumOfDays)
		Date.Day = NumOfDays;

	return Date;
}
sDate DecreaseDateByXMonths(short NumOfMonth, sDate Date)
{
	for (int i = 1; i <= NumOfMonth; i++)
	{
		Date = DecreaseDateByOneMonth(Date);
	}

	return Date;
}
sDate DecreaseDateByOneYear(sDate Date)
{
	Date.Year--;
	return Date;
}
sDate DecreaseDateByXYears(short NumOfYear, sDate Date)
{
	for (int i = 1; i <= NumOfYear; i++)
	{
		Date = DecreaseDateByOneYear(Date);
	}

	return Date;
}
sDate DecreaseDateByXYearsFaster(short NumOfYears, sDate Date)
{
	Date.Year -= NumOfYears;
	return Date;
}
sDate DecreaseDateByOneDecade(sDate Date)
{
	Date.Year -= 10;
	return Date;
}
sDate DecreaseDateByXDecades(short NumOfDecade, sDate Date)
{
	for (int i = 1; i <= NumOfDecade; i++)
	{
		Date = DecreaseDateByOneDecade(Date);
	}

	return Date;
}
sDate DecreaseDateByXDecadesFaster(short NumOfDecade, sDate Date)
{
	Date.Year -= 10 * NumOfDecade;
	return Date;
}
sDate DecreaseDateByOneCentury(sDate Date)
{
	Date.Year -= 100;
	return Date;
}
sDate DecreaseDateByOneMillennium(sDate Date)
{
	Date.Year -= 1000;
	return Date;
}
int main()
{
	sDate Date1 = ReadFullDate();

	cout << "Date After : \n\n";
	printf("%0*d_ ", 2, 1);
	Date1 = DecreaseDateByOneDay(Date1);
	cout << "Subtracting one day is: " << Date1.Day << "/" << Date1.Month << "/" << Date1.Year << "\n";

	printf("%0*d_ ", 2, 2);
	Date1 = DecreaseDateByXDays(10, Date1);
	cout << "Subtracting 10 days is: " << Date1.Day << "/" << Date1.Month << "/" << Date1.Year << "\n";

	printf("%0*d_ ", 2, 3);
	Date1 = DecreaseDateByOneWeek(Date1);
	cout << "Subtracting one week is: " << Date1.Day << "/" << Date1.Month << "/" << Date1.Year << "\n";

	printf("%0*d_ ", 2, 4);
	Date1 = DecreaseDateByXWeeks(10, Date1);
	cout << "Subtracting 10 weeks is: " << Date1.Day << "/" << Date1.Month << "/" << Date1.Year << "\n";

	printf("%0*d_ ", 2, 5);
	Date1 = DecreaseDateByOneMonth(Date1);
	cout << "Subtracting one month is: " << Date1.Day << "/" << Date1.Month << "/" << Date1.Year << "\n";

	printf("%0*d_ ", 2, 6);
	Date1 = DecreaseDateByXMonths(5, Date1);
	cout << "Subtracting 5 Months is: " << Date1.Day << "/" << Date1.Month << "/" << Date1.Year << "\n";

	printf("%0*d_ ", 2, 7);
	Date1 = DecreaseDateByOneYear(Date1);
	cout << "Subtracting one year is: " << Date1.Day << "/" << Date1.Month << "/" << Date1.Year << "\n";

	printf("%0*d_ ", 2, 8);
	Date1 = DecreaseDateByXYears(10, Date1);
	cout << "Subtracting 10 years is: " << Date1.Day << "/" << Date1.Month << "/" << Date1.Year << "\n";

	printf("%0*d_ ", 2, 9);
	Date1 = DecreaseDateByXYearsFaster(10, Date1);
	cout << "Subtracting 10 years (Faster) is: " << Date1.Day << "/" << Date1.Month << "/" << Date1.Year << "\n";

	printf("%0*d_ ", 2, 10);
	Date1 = DecreaseDateByOneDecade(Date1);
	cout << "Subtracting one decade is: " << Date1.Day << "/" << Date1.Month << "/" << Date1.Year << "\n";

	printf("%0*d_ ", 2, 11);
	Date1 = DecreaseDateByXDecades(10, Date1);
	cout << "Subtracting 10 decades is: " << Date1.Day << "/" << Date1.Month << "/" << Date1.Year << "\n";

	printf("%0*d_ ", 2, 12);
	Date1 = DecreaseDateByXDecadesFaster(10, Date1);
	cout << "Subtracting 10 decades (Faster) is: " << Date1.Day << "/" << Date1.Month << "/" << Date1.Year << "\n";

	printf("%0*d_ ", 2, 13);
	Date1 = DecreaseDateByOneCentury(Date1);
	cout << "Subtracting one Century is: " << Date1.Day << "/" << Date1.Month << "/" << Date1.Year << "\n";

	printf("%0*d_ ", 2, 14);
	Date1 = DecreaseDateByOneMillennium(Date1);
	cout << "Subtracting one Millennium is: " << Date1.Day << "/" << Date1.Month << "/" << Date1.Year << "\n";

	system("pause > 0");
	return 0;
}

```
