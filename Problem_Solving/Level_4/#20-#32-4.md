```c++
//#20to#32-4
/*
Write a program to read a date and make a functions to increase date as follows :
*IncreaseDateByXDays
* IncreaseDateByOneWeek
* IncreaseDateByXWeeks
* IncreaseDateByOneMonth
* IncreaseDateByXMonths
* IncreaseDateByOneYear
* IncreaseDateByXYears
* IncreaseDateByXYearsFaster
* IncreaseDateByOneDecade
* IncreaseDateByXDecades
* IncreaseDateByXDecadesFaster
* IncreaseDateByOneCentury
* IncreaseDateByOneMillennium
*/
#include <iostream>
#include <string>

using namespace std;

short ReadYear()
{
	short Year;

	cout << "Please enter a year? ";
	cin >> Year;

	return Year;
}
short ReadMonth()
{
	short Month;

	cout << "Please enter a Month? ";
	cin >> Month;

	return Month;
}
short ReadDay()
{
	short Day;

	cout << "\nPlease enter a Day? ";
	cin >> Day;

	return Day;
}
bool IsLeapYear(short Year)
{
	return (Year % 4 == 0 && Year % 100 != 0) || (Year % 400 == 0);
}
short NumberOfDaysInAMonth(short Month, short Year)
{
	if (Month < 1 || Month > 12)
		return 0;

	int NumberOfDays[13] = { 0,31,28,31,30,31,30,31,31,30,31,30,31 };
	return (Month == 2) ? ((IsLeapYear(Year)) ? 29 : 28) : NumberOfDays[Month];
}
int TotalDaysFromBeginingOfYear(short Day, short Month, short Year)
{
	int TotalDays = 0;

	for (int i = 1; i < Month; i++)
	{
		TotalDays += NumberOfDaysInAMonth(i, Year);
	}

	TotalDays += Day;

	return TotalDays;
}
struct sDate
{
	short Year;
	short Month;
	short Day;
};
sDate DateAddDays(short Days, sDate Date)
{
	short RemainDays = Days + TotalDaysFromBeginingOfYear(Date.Day, Date.Month, Date.Year);
	short MonthDays = 0;

	Date.Month = 1;

	while (true)
	{
		MonthDays = NumberOfDaysInAMonth(Date.Month, Date.Year);

		if (RemainDays > MonthDays)
		{
			RemainDays -= MonthDays;
			Date.Month++;

			if (Date.Month > 12)
			{
				Date.Month = 1;
				Date.Year++;
			}
		}
		else
		{
			Date.Day = RemainDays;
			break;
		}
	}

	return Date;
}
sDate ReadFullDate()
{
	sDate Date;

	Date.Day = ReadDay();
	Date.Month = ReadMonth();
	Date.Year = ReadYear();

	return Date;
}
bool IsDate1BeforeDate2(sDate Date1, sDate Date2)
{
	return (Date1.Year < Date2.Year) ? true : ((Date1.Year == Date2.Year) ? (Date1.Month < Date2.Month ? true : (Date1.Month == Date2.Month ? Date1.Day < Date2.Day : false)) : false);
}
bool IsLastDayInMonth(sDate Date)
{
	return (Date.Day == NumberOfDaysInAMonth(Date.Month, Date.Year));
}
bool IsLastMonthInYear(short Month)
{
	return (Month == 12);
}
sDate IncreaseDateByOneDay(sDate Date)
{
	if (IsLastDayInMonth(Date))
	{
		if (IsLastMonthInYear(Date.Month))
		{
			Date.Day = 1;
			Date.Month = 1;
			Date.Year++;
		}
		else
		{
			Date.Day = 1;
			Date.Month++;
		}
	}
	else
	{
		Date.Day++;
	}

	return Date;
}
sDate IncreaseDateByXDays(sDate Date, short NumOfDays)
{
	for (int i = 1; i <= NumOfDays; i++)
	{
		Date = IncreaseDateByOneDay(Date);
	}

	return Date;
}
sDate IncreaseDateByOneWeek(sDate Date)
{
	for (short i = 1; i <= 7; i++)
	{
		Date = IncreaseDateByOneDay(Date);
	}

	return Date;
}
sDate IncreaseDateByXWeeks(sDate Date, short NumOfWeek)
{
	for (short i = 1; i <= NumOfWeek; i++)
	{
		Date = IncreaseDateByOneWeek(Date);
	}

	return Date;
}
sDate IncreaseDateByOneMonth(sDate Date)
{
	if (IsLastMonthInYear(Date.Month))
	{
		Date.Month = 1;
		Date.Year++;
	}
	else
	{
		Date.Month++;
	}
	//last check day in date should not exceed max days in the current month
	// example if date is 31/1/2022 increasing one month should not be 31 / 2 / 2022, it should
    // be 28/2/2022
	short NumberOfDaysInCurrentMonth = NumberOfDaysInAMonth(Date.Month, Date.Year);
	if (Date.Day > NumberOfDaysInCurrentMonth)
	{
		Date.Day = NumberOfDaysInCurrentMonth;
	}

	return Date;
}
sDate IncreaseDateByXMonths(sDate Date, short NumOfMonth)
{
	for (short i = 1; i <= NumOfMonth; i++)
	{
		Date = IncreaseDateByOneMonth(Date);
	}

	return Date;
}
sDate IncreaseDateByOneYear(sDate Date)
{
	Date.Year++;
	return Date;
}
sDate IncreaseDateByXYears(sDate Date, short NumOfYear)
{
	for (short i = 1; i <= NumOfYear; i++)
	{
		Date = IncreaseDateByOneYear(Date);
	}

	return Date;
}
sDate IncreaseDateByXYearsFaster(sDate Date, short NumOfYear)
{

	Date.Year += NumOfYear;
	return Date;
}
sDate IncreaseDateByOneDecade(sDate Date)
{
	Date.Year += 10;
	return Date;
}
sDate IncreaseDateByXDecades(sDate Date, short NumOfDecodes)
{
	for (short i = 1; i <= NumOfDecodes; i++)
	{
		Date = IncreaseDateByOneDecade(Date);
	}

	return Date;
}
sDate IncreaseDateByXDecadesFaster(sDate Date, short NumOfDecodes)
{
	Date.Year += (NumOfDecodes * 10);
	return Date;
}
sDate IncreaseDateByOneCentury(sDate Date)
{
	Date.Year += 100;
	return Date;
}
sDate IncreaseDateByOneMillennium(sDate Date)
{
	Date.Year += 1000;
	return Date;
}
int main()
{
	sDate Date1 = ReadFullDate();
	
	cout << "Date After : \n\n";
	printf("%0*d_ ", 2, 1); 
	Date1 = IncreaseDateByOneDay(Date1);
	cout << "Adding one day is: " << Date1.Day << "/" << Date1.Month << "/" << Date1.Year << "\n";
	
	printf("%0*d_ ", 2, 2);
	Date1 = IncreaseDateByXDays(Date1,10);
	cout << "Adding 10 days is: " << Date1.Day << "/" << Date1.Month << "/" << Date1.Year << "\n";

	printf("%0*d_ ", 2, 3);
	Date1 = IncreaseDateByOneWeek(Date1);
	cout << "Adding one week is: " << Date1.Day << "/" << Date1.Month << "/" << Date1.Year << "\n";

	printf("%0*d_ ", 2, 4);
	Date1 = IncreaseDateByXWeeks(Date1,10);
	cout << "Adding 10 weeks is: " << Date1.Day << "/" << Date1.Month << "/" << Date1.Year << "\n";

	printf("%0*d_ ", 2, 5);
	Date1 = IncreaseDateByOneMonth(Date1);
	cout << "Adding one month is: " << Date1.Day << "/" << Date1.Month << "/" << Date1.Year << "\n";

	printf("%0*d_ ", 2, 6);
	Date1 = IncreaseDateByXMonths(Date1, 5);
	cout << "Adding 5 Months is: " << Date1.Day << "/" << Date1.Month << "/" << Date1.Year << "\n";

	printf("%0*d_ ", 2, 7);
	Date1 = IncreaseDateByOneYear(Date1);
	cout << "Adding one year is: " << Date1.Day << "/" << Date1.Month << "/" << Date1.Year << "\n";

	printf("%0*d_ ", 2, 8);
	Date1 = IncreaseDateByXYears(Date1, 10);
	cout << "Adding 10 years is: " << Date1.Day << "/" << Date1.Month << "/" << Date1.Year << "\n";

	printf("%0*d_ ", 2, 9);
	Date1 = IncreaseDateByXYearsFaster(Date1, 10);
	cout << "Adding 10 years (Faster) is: " << Date1.Day << "/" << Date1.Month << "/" << Date1.Year << "\n";

	printf("%0*d_ ", 2, 10);
	Date1 = IncreaseDateByOneDecade(Date1);
	cout << "Adding one decade is: " << Date1.Day << "/" << Date1.Month << "/" << Date1.Year << "\n";

	printf("%0*d_ ", 2, 11);
	Date1 = IncreaseDateByXDecades(Date1, 10);
	cout << "Adding 10 decades is: " << Date1.Day << "/" << Date1.Month << "/" << Date1.Year << "\n";

	printf("%0*d_ ", 2, 12);
	Date1 = IncreaseDateByXDecadesFaster(Date1, 10);
	cout << "Adding 10 decades (Faster) is: " << Date1.Day << "/" << Date1.Month << "/" << Date1.Year << "\n";

	printf("%0*d_ ", 2, 13);
	Date1 = IncreaseDateByOneCentury(Date1);
	cout << "Adding one Century is: " << Date1.Day << "/" << Date1.Month << "/" << Date1.Year << "\n";

	printf("%0*d_ ", 2, 14);
	Date1 = IncreaseDateByOneMillennium(Date1);
	cout << "Adding one Millennium is: " << Date1.Day << "/" << Date1.Month << "/" << Date1.Year << "\n";

	system("pause > 0");
	return 0;
}

```
