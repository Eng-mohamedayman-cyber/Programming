```c++
//#19 — Calculate the Area of a Circle Using Diameter
#include<iostream>
#include<string>
#include<cmath>
using namespace std;
float ReadRadious()
{
    float D ;
    cout<<"Please enter the diameter? "<<endl;
    cin>>D;
    return D ;
}
float CalculateCircleAreaThrouthDiameter(float D)
{
    float Area ;
    const float PI = 3.141592 ;
    Area = (PI * pow(D,2)) / 4 ;
    return Area ;
}
void PrintResult(float Area)
{
    cout<<endl<<"The area of rectangle = "<<Area<<endl;
}
int main()
{
    PrintResult(CalculateCircleAreaThrouthDiameter(ReadRadious()));
    return 0 ;
}
```
