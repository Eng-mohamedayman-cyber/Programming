```c++
//#22 — Calculate the Area of a Circle Using Triangle Sides
#include<iostream>
#include<string>
#include<cmath>
using namespace std;
void ReadTrianleData(float &A , float &B)
{
    cout<<"Please enter the A? "<<endl;
    cin>>A;
    cout<<"Please enter the B? "<<endl;
    cin>>B;
}
float CircleAreaByTriangle(float A ,float B)
{
    float Area ;
    const float PI = 3.141592 ;
    Area = PI * (pow(B,2) / 4) * ((2 * A - B)/(2 * A + B));
    return Area ;
}
void PrintResult(float Area)
{
    cout<<endl<<"The area of rectangle = "<<Area<<endl;
}
int main()
{
    float A,B;
    ReadTrianleData(A,B);
    PrintResult(CircleAreaByTriangle(A,B));
    return 0 ;
}
```
